<div class="container py-4">
    <div class="materials-form-container">
        <div class="materials-form-card">
            <div class="materials-form-header">
                <h2><i class="bi bi-plus-circle"></i> Adicionar Material de Apoio</h2>
            </div>
            <div class="materials-form-body">
                <div class="form-deck-info">
                    <p><strong>Deck:</strong> <?= htmlspecialchars($deck->name) ?></p>
                </div>

                <form action="<?= route('materials.create', ['deck_id' => $deck->id]) ?>"
                      method="POST"
                      enctype="multipart/form-data">
                    <input type="hidden" name="page" value="<?= $returnPage ?? 1 ?>">
                    <input type="hidden" name="sort" value="<?= htmlspecialchars($returnSort ?? 'created_desc') ?>">

                    <div class="form-group">
                        <label for="material_title" class="form-label">
                            Título do Material <span class="text-danger">*</span>
                        </label>
                        <input type="text"
                               class="form-control <?= $material->errors['title'] ?? false ? 'is-invalid' : '' ?>"
                               id="material_title"
                               name="material[title]"
                               value="<?= htmlspecialchars($material->title ?? '') ?>"
                               placeholder="Ex: Resumo de Matemática - Funções">
                        <?php if (isset($material->errors['title'])): ?>
                            <div class="invalid-feedback">
                                <?= $material->errors['title'] ?>
                            </div>
                        <?php endif; ?>
                    </div>

                    <div class="form-group">
                        <label for="material_file" class="form-label">
                            Arquivo PDF <span class="text-danger">*</span>
                        </label>
                        <input type="file"
                               class="form-control <?= $material->errors('file') ? 'is-invalid' : '' ?>"
                               id="material_file"
                               name="material_file"
                               accept="application/pdf">
                        <?php if ($material->errors('file')): ?>
                            <div class="invalid-feedback">
                                <?= $material->errors('file') ?>
                            </div>
                        <?php endif; ?>
                        <small class="form-text">
                            <i class="bi bi-info-circle"></i> Apenas arquivos PDF são aceitos. Tamanho máximo: 20MB
                        </small>
                    </div>

                    <div class="form-tip">
                        <p>
                            <i class="bi bi-lightbulb"></i>
                            <strong>Dica:</strong> Materiais de apoio são ótimos para complementar seus estudos.
                            Você pode adicionar resumos, apostilas, ou qualquer conteúdo em PDF relacionado a este deck.
                        </p>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-upload"></i> Adicionar Material
                        </button>
                        <a href="<?= route('materials.index', ['deck_id' => $deck->id, 'page' => $returnPage ?? 1, 'sort' => $returnSort ?? 'created_desc']) ?>"
                           class="btn btn-secondary">
                            <i class="bi bi-x-circle"></i> Cancelar
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

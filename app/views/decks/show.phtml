<div class="container py-4">
    <div class="deck-details-container">
        <div class="deck-details-header">
            <?php 
            $from = $_GET['from'] ?? '';
            $backUrl = $from === 'shared' ? route('shared.decks.index', ['page' => $returnPage ?? 1]) : route('decks.index') . '?page=' . ($returnPage ?? 1) . '&sort=' . urlencode($returnSort ?? 'created_desc');
            ?>
            <a href="<?= $backUrl ?>" class="back-arrow">
                <i class="bi bi-arrow-left"></i>
            </a>
            <div class="deck-details-title">
                <h2><?= htmlspecialchars($deck->name) ?></h2>
                <p class="deck-details-description"><?= htmlspecialchars($deck->description) ?></p>
            </div>
        </div>

        <?php if ($totalCards > 0 && ($newCards > 0 || $dueCards > 0)): ?>
            <div class="deck-stats-container">
                <div class="deck-stat-card stat-new">
                    <div class="stat-label">Novos</div>
                    <div class="stat-value"><?= $newCards ?></div>
                </div>
                <div class="deck-stat-card stat-due">
                    <div class="stat-label">Revisar</div>
                    <div class="stat-value"><?= $dueCards ?></div>
                </div>
                <div class="deck-stat-card stat-total">
                    <div class="stat-label">Total</div>
                    <div class="stat-value"><?= $totalCards ?></div>
                </div>
            </div>

            <div class="study-button-container">
                <a href="<?= route('study.start', ['id' => $deck->id]) ?>" class="btn-study-now">
                    <i class="bi bi-play-circle-fill"></i> Estudar Agora
                </a>
            </div>
        <?php else: ?>
            <div class="congratulations-container">
                <div class="congratulations-icon">
                    <i class="bi bi-trophy-fill"></i>
                </div>
                <h3>Parabéns!</h3>
                <p>Você terminou este deck por enquanto.</p>
                <?php if ($totalCards === 0): ?>
                    <?php if ($deck->user_id === $this->currentUser()->id): ?>
                        <p class="small-text">Este deck ainda não possui cards. <a href="/cards/new?deck_id=<?= $deck->id ?>&return_deck_id=<?= $deck->id ?>&page=<?= $returnPage ?? 1 ?>">Adicione alguns cards</a> para começar a estudar!</p>
                    <?php else: ?>
                        <p class="small-text">Este deck ainda não possui cards.</p>
                    <?php endif; ?>
                <?php else: ?>
                    <p class="small-text">Volte mais tarde para revisar seus cards.</p>
                <?php endif; ?>
            </div>
        <?php endif; ?>

        <div class="deck-info-summary">
            <h4><i class="bi bi-info-circle"></i> Informações do Deck</h4>
            <div class="info-grid">
                <div class="info-item">
                    <span class="info-label">Total de Cards:</span>
                    <span class="info-value"><?= $totalCards ?></span>
                </div>
                <div class="info-item">
                    <span class="info-label">Cards Novos:</span>
                    <span class="info-value"><?= $newCards ?></span>
                </div>
                <div class="info-item">
                    <span class="info-label">Para Revisar:</span>
                    <span class="info-value"><?= $dueCards ?></span>
                </div>
            </div>
        </div>
    </div>
</div>

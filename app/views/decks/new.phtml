<div class="deck-form-container">
    <div class="deck-form-wrapper">
        <div class="deck-form-header">
            <h2><i class="bi bi-collection"></i> Criar Novo Deck</h2>
            <p>Organize seus estudos criando baralhos personalizados</p>
        </div>

        <form id="deck_create" action="/decks" method="POST" class="deck-form <?= $deck->hasErrors() ? 'was-validated' : '' ?>" novalidate>
            <div class="form-group-deck">
                <label for="deck_name">
                    <i class="bi bi-card-heading"></i> Nome do Deck
                </label>
                <input
                    class="form-input-deck <?= ($deck->hasErrors() && $deck->errors('name')) ? 'is-invalid' : '' ?>"
                    type="text"
                    id="deck_name"
                    name="deck[name]"
                    placeholder="Ex: Inglês - Verbos Irregulares"
                    required
                    maxlength="100"
                    value="<?= htmlspecialchars($deck->name ?? '') ?>">
                <?php if ($deck->hasErrors() && $deck->errors('name')): ?>
                    <div class="invalid-feedback" style="display: block !important;"><?= $deck->errors('name') ?></div>
                <?php endif; ?>
            </div>

            <div class="form-group-deck">
                <label for="deck_description">
                    <i class="bi bi-text-paragraph"></i> Descrição
                </label>
                <textarea
                    class="form-input-deck <?= ($deck->hasErrors() && $deck->errors('description')) ? 'is-invalid' : '' ?>"
                    id="deck_description"
                    name="deck[description]"
                    placeholder="Descreva o conteúdo e objetivo deste deck..."
                    required
                    maxlength="500"><?= htmlspecialchars($deck->description ?? '') ?></textarea>
                <?php if ($deck->hasErrors() && $deck->errors('description')): ?>
                    <div class="invalid-feedback" style="display: block !important;"><?= $deck->errors('description') ?></div>
                <?php endif; ?>
            </div>

            <div class="form-tip">
                <p>
                    <i class="bi bi-lightbulb"></i>
                    <strong>Dica:</strong> Dê um nome claro e descritivo ao seu deck. Isso facilitará encontrá-lo depois!
                </p>
            </div>

            <div class="form-actions-deck">
                <a href="/decks" class="btn-deck btn-deck-secondary">
                    <i class="bi bi-arrow-left"></i> Cancelar
                </a>
                <button type="submit" class="btn-deck btn-deck-primary">
                    <i class="bi bi-check-circle"></i> Criar Deck
                </button>
            </div>
        </form>
    </div>
</div>
<link href="/assets/css/application.css" rel="stylesheet">

<div class="container mt-5">
    <div class="row">
        <div class="col-12 text-center">
            <div class="p-5 rounded shadow bg-dark text-white">
                <h2 class="mb-0">
                    <i class="bi bi-stack"></i> Meus Decks
                </h2>
                <p class="mt-3 text-secondary">Bem-vindo, <?= htmlspecialchars($user->name) ?>!</p>

                <?php if (!empty($decks)): ?>
                    <?php foreach ($decks as $deck): ?>
                        <div class="deck-card" style="border: 1px solid #ccc; padding: 10px; margin-bottom: 10px;">
                            <h2><?= htmlspecialchars($deck->name) ?></h2>
                            <p><?= htmlspecialchars($deck->description) ?></p>
                            <form
                                action="/decks/<?= $deck->id ?>/delete"
                                method="POST"
                                style="display: inline;"
                                onsubmit="return confirm('Tem certeza que deseja excluir este deck?');">
                                <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i></button>
                            </form>
                        </div>
                    <?php endforeach; ?>
                <?php else: ?>
                    <p>Nenhum deck encontrado. <a href="/decks/create">Crie o seu primeiro deck!</a></p>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>
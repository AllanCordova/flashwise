<div class="container py-4">
    <div class="decks-container">
        <div class="decks-header">
            <div>
                <h2><i class="bi bi-collection"></i> Meus Decks</h2>
                <p class="text-secondary mb-0">Bem-vindo, <?= htmlspecialchars($this->currentUser()->name) ?>!</p>
            </div>
            <div class="decks-header-actions">
                <?php
                $currentPage = isset($_GET['page']) ? (int)$_GET['page'] : 1;
                $currentSort = $_GET['sort'] ?? 'created_desc';
                ?>
                <a href="<?= route('cards.new') ?>?page=<?= $currentPage ?>&sort=<?= urlencode($currentSort) ?>" class="create-deck-btn">
                    <i class="bi bi-plus-circle"></i> <span>Adicionar Card</span>
                </a>
                <a href="<?= route('decks.new') ?>?page=<?= $currentPage ?>&sort=<?= urlencode($currentSort) ?>" class="create-deck-btn">
                    <i class="bi bi-plus-circle"></i> <span>Criar Novo Deck</span>
                </a>
            </div>
        </div>

        <?php if ($paginator->totalOfRegisters() > 0): ?>
            <!-- Filtro de Ordenação -->
            <div class="decks-filters">
                <div class="filter-group">
                    <label for="sort-select">
                        <i class="bi bi-funnel"></i> Ordenar por:
                    </label>
                    <select id="sort-select" class="filter-select" onchange="window.location.href='/decks?sort=' + this.value + '&page=1'">
                        <option value="review_priority" <?= ($currentSort ?? '') === 'review_priority' ? 'selected' : '' ?>>
                            Prioridade de Revisão
                        </option>
                        <option value="created_desc" <?= ($currentSort ?? 'created_desc') === 'created_desc' ? 'selected' : '' ?>>
                            Mais Recentes
                        </option>
                        <option value="created_asc" <?= ($currentSort ?? '') === 'created_asc' ? 'selected' : '' ?>>
                            Mais Antigos
                        </option>
                        <option value="name_asc" <?= ($currentSort ?? '') === 'name_asc' ? 'selected' : '' ?>>
                            Nome (A-Z)
                        </option>
                        <option value="name_desc" <?= ($currentSort ?? '') === 'name_desc' ? 'selected' : '' ?>>
                            Nome (Z-A)
                        </option>
                    </select>
                </div>
            </div>

            <table class="decks-table">
                <thead>
                    <tr>
                        <th>Nome do Deck</th>
                        <th style="text-align: center;">Novas</th>
                        <th style="text-align: center;">Revisar</th>
                        <th style="text-align: center;">Total</th>
                        <th style="text-align: right;">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($paginator->registers() as $deck): ?>
                        <?php
                        $currentPage = isset($_GET['page']) ? (int)$_GET['page'] : 1;
                        $currentSort = $_GET['sort'] ?? 'created_desc';
                        ?>
                        <tr class="deck-row-clickable" onclick="window.location.href='/decks/<?= $deck->id ?>?page=<?= $currentPage ?>&sort=<?= urlencode($currentSort) ?>';" style="cursor: pointer;">
                            <td>
                                <div class="deck-name"><?= htmlspecialchars($deck->name) ?></div>
                                <div class="deck-description"><?= htmlspecialchars($deck->description) ?></div>
                            </td>
                            <td style="text-align: center;">
                                <div class="stat-item">
                                    <span class="stat-number stat-new"><?= $deck->countNewCards($this->currentUser()->id) ?></span>
                                </div>
                            </td>
                            <td style="text-align: center;">
                                <div class="stat-item">
                                    <span class="stat-number stat-due"><?= $deck->countDueCards($this->currentUser()->id) ?></span>
                                </div>
                            </td>
                            <td style="text-align: center;">
                                <div class="stat-item">
                                    <span class="stat-number stat-total"><?= $deck->countTotalCards() ?></span>
                                </div>
                            </td>
                            <td onclick="event.stopPropagation();">
                                <div class="deck-actions">
                                    <a class="action-btn btn-materials" href='<?= route('materials.index', ['deck_id' => $deck->id, 'page' => $currentPage, 'sort' => $currentSort]) ?>' title="Materiais de apoio">
                                        <i class="bi bi-file-earmark-pdf"></i>
                                    </a>
                                    <button class="action-btn btn-share" onclick="shareDeck(<?= $deck->id ?>)" title="Compartilhar deck">
                                        <i class="bi bi-share"></i>
                                    </button>
                                    <a class="action-btn btn-edit" href='<?= route('decks.edit', ['id' => $deck->id, 'page' => $currentPage]) ?>?sort=<?= urlencode($currentSort) ?>' title="Editar deck">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <form
                                        action="<?= route('decks.destroy', ['id' => $deck->id]) ?>"
                                        method="POST"
                                        style="display: inline;"
                                        onsubmit="return confirm('Tem certeza que deseja excluir este deck?');">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="page" value="<?= $currentPage ?>">
                                        <input type="hidden" name="sort" value="<?= htmlspecialchars($currentSort) ?>">
                                        <button type="submit" class="action-btn btn-delete" title="Excluir deck">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>

            <div class="pagination-links">
                <?php $paginator->renderPagesNavigation();
                ?>
            </div>


        <?php else: ?>
            <div class="no-decks">
                <i class="bi bi-inbox"></i>
                <h3>Nenhum deck encontrado</h3>
                <p>Comece criando seu primeiro deck de estudos!</p>
                <?php
                $currentPage = isset($_GET['page']) ? (int)$_GET['page'] : 1;
                $currentSort = $_GET['sort'] ?? 'created_desc';
                ?>
                <a href="<?= route('decks.new') ?>?page=<?= $currentPage ?>&sort=<?= urlencode($currentSort) ?>" class="create-deck-btn" style="display: inline-block; margin-top: 1rem;">
                    <i class="bi bi-plus-circle"></i> Criar Primeiro Deck
                </a>
            </div>
        <?php endif; ?>
    </div>
</div>